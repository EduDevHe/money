import type { NextPage } from "next";
import Head from "next/head";

const Home: NextPage = () => {
	return (
		<>
			<Head>
				<title>Money</title>
				<meta name="description" content="Take care of your money" />
				<link rel="icon" href="/favicon.ico" />
			</Head>

			<main>
				<h1>Money</h1>
			</main>
		</>
	);
};

export default Home;
